#include<bits/stdc++.h>
using namespace std;

using ll=long long;
const ll MOD=1e9+7;
// all m must be coprime each other.
// O(K^2), where K is size of b and m
// return 
ll crt(vector<ll> b,vector<ll> m){
    for(int i=0;i<)
    
}